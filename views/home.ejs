<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Camp-Around</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    :root {
      --dark-bg: #1e1e1e;
      --medium-dark: #2c2c2c;
      --accent: #00b894; /* teal green */
      --text-light: #f0f0f0;
    }

    body {
      font-family: 'Segoe UI', sans-serif;
      background-color: var(--dark-bg);
      color: var(--text-light);
    }

    .hero {
      background-color: var(--medium-dark);
      color: var(--text-light);
      padding: 5rem 0;
      text-align: center;
      box-shadow: 0 8px 20px rgba(0, 0, 0, 0.4);
    }

    .hero h1 {
      font-size: 3.5rem;
      font-weight: 700;
    }

    .hero p {
      font-size: 1.2rem;
      color: #ccc;
    }

    .btn-accent {
      background-color: var(--accent);
      color: #fff;
      border: none;
    }

    .btn-accent:hover {
      background-color: #00a383;
    }

    .features {
      padding: 4rem 0;
    }

    .card {
      background-color: var(--medium-dark);
      color: var(--text-light);
      border: none;
      border-radius: 0.75rem;
      transition: transform 0.2s ease, box-shadow 0.2s ease;
      box-shadow: 0 5px 12px rgba(0, 0, 0, 0.3);
    }

    .card:hover {
      transform: translateY(-5px);
      box-shadow: 0 10px 20px rgba(0, 0, 0, 0.6);
    }

    .card-title {
      color: var(--accent);
    }

    .footer {
      background-color: #121212;
      color: #aaa;
      padding: 1.5rem 0;
    }

    .footer span {
      font-size: 0.95rem;
    }
  </style>
</head>

<body class="d-flex flex-column min-vh-100">
  <%- include('./partials/navbar') %>

  <!-- HERO SECTION -->
  <header class="hero">
    <div class="container">
      <h1>Welcome to Camp-Around</h1>
      <p>Discover the outdoors. Spot the vibe. Camp the moment.</p>
      <a href="/campgrounds" class="btn btn-accent btn-lg mt-4 px-4">Explore Campsites</a>
    </div>
  </header>

  <!-- FEATURES SECTION -->
  <main class="container features text-center">
    <h2 class="mb-5 text-accent">Why Camp-Around?</h2>
    <div class="row g-4">
      <div class="col-md-4">
        <div class="card p-4 h-100">
          <div class="card-body">
            <h5 class="card-title">🧭 Discover</h5>
            <p class="card-text">Find hidden gems and top-rated spots reviewed by real users.</p>
          </div>
        </div>
      </div>
      <div class="col-md-4">
        <div class="card p-4 h-100">
          <div class="card-body">
            <h5 class="card-title">📸 Share</h5>
            <p class="card-text">Post your own experiences and help others plan better hangouts.</p>
          </div>
        </div>
      </div>
      <div class="col-md-4">
        <div class="card p-4 h-100">
          <div class="card-body">
            <h5 class="card-title">🌐 Connect</h5>
            <p class="card-text">Join a vibrant community of nature lovers and outdoor explorers.</p>
          </div>
        </div>
      </div>
    </div>
  </main>

  <%- include('./partials/footer') %>

  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.8/dist/umd/popper.min.js" crossorigin="anonymous"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.min.js" crossorigin="anonymous"></script>
</body>

</html>

